Time Request Retracted

======================

Hello, <%= @approving_user.username %>

<%= @retracting_user.username %> has retracted their <%= @time_request.time request_type ? @time_request.time_request_type.name : "" %> schedule request <%= @time_request.name %> from <%= @time_request.from.strftime("%m/%d/%Y %H:%M") %> to <%= @time_request.to.strftime("%m/%d/%Y %H:%M") %>.